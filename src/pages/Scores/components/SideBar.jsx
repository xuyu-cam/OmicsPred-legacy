import React from "react";

import BlogRoutes from "../../../BlogRoutes";

const pages = ["first", "second", "third", "fort"];

const SideBar = (props) => {
  return (
    <div className="w-[20%] pt-5 p- h-screen bg-indigo-600">
      {pages.forEach((p) => {
        <div className="p-4 flex rounded-xl shadow-md mt-3 bg-white ">
          value.name
        </div>;
      })}
    </div>
  );
};

export default SideBar;
